import { AdminDashboard } from "@/components/admin/admin-dashboard";

export const metadata = {
  title: "Admin | M.A.W Printing",
  description: "Manage customer orders.",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
