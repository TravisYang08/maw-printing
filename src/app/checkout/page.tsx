import { CheckoutForm } from "@/components/checkout/checkout-form";

export const metadata = {
  title: "Checkout | M.A.W Printing",
  description: "Complete your custom print order.",
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
